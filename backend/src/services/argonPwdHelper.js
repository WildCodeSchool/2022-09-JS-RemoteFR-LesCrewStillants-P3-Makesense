const argon2 = require("argon2");



/**
 * @doc hashPassword
 * Permet de hasher le mot de passe user
 * hashPassword à 1 parametre :
 * - password qui est le pwd à hasher
 */

function hashPassword(password) {
  argon2

}

module.exports = {
  hashPassword,
};
